package com.focus.userModel.lists.workContainers;

import com.focus.userModel.lists.WorkContainer;

public class Epic extends WorkContainer {
    public Epic(String title, int expValue) {
        super(title, expValue);
    }
}
